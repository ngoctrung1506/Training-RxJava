<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only needed to save the edited picture on Android 9 and older; from Android 10
         on, MediaStore writes into the gallery without any permission. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".editor.PhotoEditorActivity"
            android:label="@string/photo_editor"
            android:theme="@style/AppTheme.Editor" />
        <activity android:name=".operator.combine.ConcatExampleActivity" />
        <activity android:name=".operator.combine.MergeExampleActivity" />
        <activity android:name=".operator.transfrom.BufferExampleActivity" />
        <activity android:name=".operator.filter.SkipExampleActivity" />
        <activity android:name=".operator.filter.FilterExampleActivity" />
        <activity android:name=".operator.transfrom.SwitchMapExampleActivity" />
        <activity android:name=".operator.transfrom.MapExampleActivity" />
        <activity android:name=".operator.create.SimpleExampleActivity" />
        <activity android:name=".OperatorsActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>